# Birthday Card

A responsive birthday card built using HTML and CSS. Media queries, % and rem units are used to make the card, text, and buttons adapt to different screen sizes. This project shows my progress so far—still learning more CSS concepts, but this reflects what I’ve applied in practice.
